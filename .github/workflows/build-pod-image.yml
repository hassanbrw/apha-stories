name: Build pod Docker image

on:
  push:
    branches: [main]
    paths:
      - 'pod/**'
      - 'stages/**'
      - 'lib/**'
      - 'run.js'
      - 'package.json'
  workflow_dispatch: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./pod/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/story-alpha-pod:latest
